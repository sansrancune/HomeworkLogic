Debut Ex9.6

Debut declaration variables
var taille: N<-1
var matrice[taille][taille]
var i: N<-1
var j: N<-1
var reste: N<-0
var x: N<-0
var length: N<-0
var height: N<-0
var pos:<-1
fin declaration variables

Afficher " saisir taille de la matrice "
saisir taille

Si length>height
	alors x<-length div height
	reste<-length%height
	sinon x<- height div length
	reste<- height%length
fin si

Pour i allant de 1 a height-1 a pas de 1 
Pour j allant de pos a pos+x a pas de 1 
matrice[i][j]<-1
j suivant
pos<-pos+x
i suivant

Pour j allant de length-reste a length a pas de 1
matrice[height][j]<- 1
j suivant

Fin Ex9.6